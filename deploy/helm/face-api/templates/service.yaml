apiVersion: v1
kind: Service
metadata:
  name: {{ include "face-api.fullname" . }}
  labels:
    {{- include "face-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "face-api.selectorLabels" . | nindent 4 }}